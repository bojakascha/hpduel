# USER MGMT

## General
- Default Random "unmanaged" user name created, e.g. Spelare_128 (does not need to be 100% unique)
  - Creates unique id in firebase asap
- All functionality available
  - duel
  - leaderboard (later func)
  - history, stats (later func)
  - managed user / log in only needed when:
    - user state needs to be synched across devices or updates
  - Creating a new "managed" user keeps the current unmanged users state
  - Should be possible to change name anytime
  - Name not tied to identity

## User/profile page
- Shows name, editable
- Then login etc.
- Or current account + logout
- Possibility to delete user data

### Friends ((Collapsed section in Profile page?))
- Possible to invite friends by name
- Possible to invite friends from past games
- Possible to accept/ignor invites

### History (Collapsed section in Profile page?)
- Shows summary of past games